# -2026-knowledge-base-
Personal workspace for experimenting with algorithms and problem-solving. Contains code snippets, logic exercises, and technical notes collected while studying. Files are encoded to prevent browser rendering issues with special characters. A training ground for self-assessment and quick reference, not a production project

## temp_store.enc
This file contains cached API responses from a mock testing service.  
To view: `base64 -d temp_store.enc | less` (requires decoding, contains special chars
